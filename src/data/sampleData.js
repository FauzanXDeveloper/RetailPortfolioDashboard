/**
 * Data source defaults for the analytics dashboard.
 * No built-in sample data — users import their own data via Manage Data.
 */

/**
 * Default data sources — empty by default.
 * Users add data sources via the Data Manager (import CSV, Excel, JSON, or API).
 */
export const defaultDataSources = [];
